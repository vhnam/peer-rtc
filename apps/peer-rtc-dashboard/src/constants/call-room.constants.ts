export const LOCAL_MEDIA_WIDTH = 240;
export const LOCAL_MEDIA_HEIGHT = (LOCAL_MEDIA_WIDTH * 9) / 16;
/** Matches Tailwind `bottom-4` / `right-4` inset inside the stage droppable. */
export const LOCAL_MEDIA_INSET = 16;

export const CALL_ROOM_STAGE_DROPPABLE_ID = 'draggable-container';
export const CALL_ROOM_LOCAL_MEDIA_DRAGGABLE_ID = 'local-media';

export const DEFAULT_STAFF_DISPLAY_NAME = 'Staff';

export const CONSUMER_PICKUP_TIMEOUT_MS = 30_000;
